{
  "name": "rampage",
  "version": "0.0.0",
  "description": "A generic way to split an array into pages, with optional callbacks to modify the structure of each page.",
  "author": "Lim Yuan Qing",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/yuanqing/rampage.git"
  },
  "dependencies": {
    "cheque": "^0.1.1"
  },
  "devDependencies": {
    "istanbul": "^0.3.5",
    "jshint": "^2.5.11",
    "tape": "^3.0.3"
  },
  "scripts": {
    "lint": "node_modules/.bin/jshint --verbose index.js test/*.js",
    "test": "rm -rf coverage && node_modules/.bin/istanbul cover tape test/*.js"
  },
  "keywords": [
    "array",
    "chunk",
    "collection",
    "object",
    "page",
    "paginate",
    "pagination"
  ]
}
